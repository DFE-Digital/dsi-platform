@inject IServiceNavigationBuilder serviceNavigationBuilder

@{
    Layout = "_DsiLayout";

    ViewData[DsiLayoutConstants.ServiceName] = "DfE Sign-in Help centre";
    ViewData[DsiLayoutConstants.ServiceNavigationItems] = await serviceNavigationBuilder.BuildAsync(ViewContext.HttpContext.Request.Path);
}
